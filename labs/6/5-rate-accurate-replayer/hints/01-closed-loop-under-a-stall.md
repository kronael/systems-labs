> Spoilers. Open only when stuck.

# What a design that waits for each answer would report during a stall

What a design that waits for each answer before sending the next would
silently report instead, when the target stops answering.

This is a question your `ARCHITECTURE.md` must answer. It sits here rather
than in the task because stating it there would point at the answer.
