# Bugs

The queue is empty as of 2026-09-12.

The seven findings that stood here were shipped rather than carried: `S1`,
`S26` through `S32`. What they amounted to was one defect — four labs asked a
local emulator to produce the limit their lesson lives in, and an emulator
reproduces an API and omits its limits. `specs/01-systems-labs.md` now states
that a barrier holds execution rather than being noticed afterwards, and that
the controller supplies the limit at a transport, process, or clock layer;
every lab whose gate depends on a limit names its layer and says that the gate
proves a declared model.

The resolutions are in `.diary/20260912.md` with their commits.

Record a defect here rather than fixing it in passing — see `CONTRIBUTING.md`,
"Filing a defect". A fix that changes a contract, control flow, or anything
cross-cutting is a redesign: record it as a proposal and get sign-off first.
