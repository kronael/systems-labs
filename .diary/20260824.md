---
summary: |
  Systems Labs curriculum. Specs only, no code. Master spec still `draft`.
  - Bug queue worked down from eight open to three; all three need the owner
  - S8, S11, S14, S15, S17, S18 closed 2026-08-24
  - S1, S9, S10 still await sign-off — all are owner decisions, not defects
  - `01-systems-labs.md` still `draft`; implementation blocked until `accepted`
---

## 18:26 2026-08-24

Bug sweep. Four agents on disjoint phase directories (3+4, 6, 7, 8), commits
held by the parent so four concurrent writers could not race the git index.
Shared files — `01`, `0/`, `BUGS.md` — kept for myself.

S18 was the one worth thinking about. Every point of the finding was correct:
the history record had no field for `never` or `bounded` to range over,
"ordered" had no cross-emitter rule, and build-order item one had no directory
while every other item did. But its proposal — versioned event schema, clock
domains, merge rule, observation adapters, classification of 31 contracts — is
machinery for a compiled checker that S16 and the grader removal already
deleted. Cut the claim instead of building the schema. The five shapes stay as
words an answer key uses, each ranging over whatever that lab's own boundaries
expose, which is why `never` and `bounded` need no record field at all.

S8 turned out to be an unwritten rule rather than an inconsistency: the lab
fixes an admission ceiling when it is an environment fact needed to guarantee
the regime, the learner declares it when choosing it is the design decision.

S15 closed by decision and says plainly it is weaker than a gate. No simple
design remains to run and time, so the earned-dependency rule lives as step 3 of
the authoring procedure.

S17 was scoped wrong in its own entry. It read as phases 3 to 8; phases 1 and 2
carried twenty-seven references the grader removal never touched, including
every prepared-scaffold list promising a black-box grader that does not exist.
Fixed by reading each sentence rather than renaming — "grader" did the fault
controller's job in one sentence and the check's in the next.

S11's hour restatements are the substantive change. `7/6` at twenty to
twenty-five, `4/5` at eighteen to twenty-four, `6/3` at sixteen to twenty-two.
The old figures summed the three gates and charged nothing for the
falsify-and-rebuild loop, which is where most of the hours go.

Two agent calls needed correcting. The phase 3+4 agent flagged its own
"materializer topology" as borderline and was right — it names a mechanism
shape, not a category. The phase 8 agent noticed nobody owned `specs/0/`, where
four more stale references were sitting.
