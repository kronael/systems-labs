---
summary: |
  systems-labs, specs only. Today: the teaching contract tightened and the
  starter menu removed.
  - S26 half open — seventeen specs still promise neighbour links they lack
  - S1, S9, S10, S25 still proposed, all needing sign-off
  - 7/7's track row calls it program development; its spec has no program
  - user works 7/4 next, Go starter, ARCHITECTURE.md before code
---

## 12:05

Contract change, commit c6ecc0f. Every `Code pointers` citation is
solution-bearing. The page that reports a behaviour states it, so citing it in
`README.md` handed over the reading. README keeps requirements, dataset
provenance, neighbour links.

## 12:06

Each brief now names one starter instead of a menu, and Go is it unless the
environment dictates otherwise — Rust and C for phase 6, the chain's language
for an on-chain program, TypeScript for 7/6's browser bundle. Commits e3f6da5,
16c18e9.

## 12:07

Closed the `0/5` open question: `template/` carries Go only. A second starter
doubles the surface the scaffold must keep passing.

## 13:10

Closed the seventeen-spec neighbour-link gap, commit 36e7e17. Every neighbour
in the catalog now carries a documentation URL that answered 200 today, and
nine category names became products.

## 13:25

Closed the mechanism leaks: four specs mark their questions `HINTS.md`-bound,
five state the property instead of naming the mechanism. Commits 67c2b49,
fb3afaa, fe3fbf2. `S26` now has one finding left — eleven labs fire faults
with no named barrier, which is per-lab design work.

## 13:55

Closed the last mechanical finding in `S26`: eleven labs fired faults on a
timer or on a vague condition. Every scenario now names its barrier, and each
"around its durable effect" is split in two, because "around" cannot land at
the same boundary twice. Commits 6a1d717, 52eebf8.
