---
summary: |
  systems-labs, specifications only. Today: root files cut back and the README
  rewritten in the author's own voice.
  - `01-systems-labs.md` is still `draft`, so implementation stays blocked
  - CLAUDE.md is 428 lines against its own 200-line rule
---

## 14:10

Cut `THIRD_PARTY.md`. It held one row, `LICENSE` itself, and the GPL-3.0 text
carries its own notice. The attribution model in `01-systems-labs.md` now adds
a root ledger with the first commit that copies anything else, so the
obligation survives without a file that records nothing. The same edit drops
the promised root `NOTICE`, which never existed. Commit 2568fc1.

## 14:12

`BUGS.md` is the queue again, seven lines. The account of what the last sweep
fixed lives in `.diary/20260912.md`.

## 14:30

Rewrote `README.md`. It now opens with who writes it: a language model writes
most of the prose, the motive and the judgement are the author's, and it
matures as the work goes on. The rest is the same content without the borrowed
product-page register. Commit b61411e.

## 15:40

Four method reviews, one per phase group, against predict-provoke-observe.
Fifteen labs support it; nineteen had a gap. `S33` to `S36` record them. Fixed
the four that broke something: `4/1` published no limit to predict, `1/5` and
`7/7` failed a correct design at an unmapped barrier, `3/2` fired "around" an
effect, `8/1` asked about surviving shards on a single node.

## 16:20

Hints became a directory, one file per hint with an index, and a failure mode
became a hint. The learner README now states the product, the requirements,
the limits and the evidence, and never how a design fails.

## 17:05

`specs/` is deleted. Each lab directory holds its task, its `hints/`, and
`EVALUATION.md` with the failure schedule and what each scenario must leave
true. The contract moved to `docs/contract.md` and the scaffold to
`docs/shared-scaffold.md`; the catalog merged into `labs/README.md`; each phase
page sits at `labs/<phase>/README.md`. Every reference and every relative link
was rewritten and checked: 113 broken, then zero.

The deterrence standard changed with it, and the contract says so now. The
schedule is no longer absent from the tree; it is behind a spoiler warning in a
file opened by choice, the same protection the hints have. What runs is still
the compiled recipe.
